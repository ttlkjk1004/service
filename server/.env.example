# Server Environment Variables
PORT=3000
NODE_ENV=production

# Database (Railway will provide this automatically if using PostgreSQL)
# For SQLite in production (not recommended for production, but works for testing)
DATABASE_PATH=./service_reports.db
